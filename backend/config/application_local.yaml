# 本文件保存项目运行变量及模型环境变量映射，后续配置可直接扩展。
default_tenant: "233"

tenants:
  "233":
    default_user: dbh
    session_name: pe

current_model: KIMI-K26

models:
  KIMI-K26:
    api_key_env: KIMI-K26_API_KEY
    base_url_env: KIMI-K26_BASE_URL
    model_name_env: KIMI-K26_MODEL_NAME
    parameters:
      max_completion_tokens: 64
      extra_body:
        thinking:
          type: disabled
